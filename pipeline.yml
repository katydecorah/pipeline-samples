pr:
- main

resources:
  repositories:
  - repository: 1esPipelines
    type: git
    name: Domino/1ESPipelines
    ref: dev/katydecorah/refactor-build-tags-part-2
    #ref: refs/tags/release

extends:
  template: v1/1ES.Unofficial.PipelineTemplate.yml@1esPipelines
  parameters:
    #settings:
        #skipBuildTagsForGitHubPullRequests: true
    pool:
      name: pt-validation-dev
      image: pt-validation-dev-windows2019
      os: windows

    stages:
    - stage: Stage
      jobs:
      - job: job
        steps:
        - powershell: echo "Hello World from host"
